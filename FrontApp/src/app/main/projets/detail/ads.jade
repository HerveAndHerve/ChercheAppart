section
  div.md-subheader.md-default-theme
    div.md-subheader-content
      h2 Annonces 

  md-list.lands-list( layout="column" )
    md-item( 
      ng-repeat="ad in ads"
    )
      md-divider
      md-item-content( style="background-color: white" )
        div.md-tile-left.md-image-tile
          img( ng-src="{{ ad.img }}" style="width: 120px; margin: 10px")
        div.md-tile-content( layout="row" layout-align="space-between center" )
          div
            h3( ng-click="selectAd(ad)" ) {{ ad.location }} - {{ ad.district }}
            p {{ ad.description }}
            p {{ ad.price }} &euro;
            p {{ ad.surface | number }} m
              sup 2
          div( flex )
          div
            md-button.md-primary.md-raised( ng-click="interestingAd(ad)" )
              span.fa.fa-star.fa-2x


